i want you to build a react website for medical 
reservation with ai chat  which askes some questions then gives doctor
  suggetions with the same speciality and nearby 
   the style is from tailwind
  contains :
  - homepage  (
    navbar if the ueser is logged in it shows its name and its role)
    the navbar is in many pages

    modern design 

    the hero (in the left side there is a 3d human body with animation rotating , and some text on the right side , 
    and a button to go to the ai chat page , and a button to go to the login page )

    the next section is a section to show the services .
    the next section is new medical news which will be come from outer api,

    the next section is a search bar to search for doctors by speciality and location and name,

    the next section is a list of doctors with their speciality-it displays the doctor's name, speciality(icon and name), and there location, and a button to book an appointment which will redirect to the reservation page


  - dashboard for  doctors (appointments, patients, schedule, etc.)
  - dashboard for patients (appointments, schedule, etc.)
  - dashboard for admins (accept a new doctor, manage appointments, manage patients, manage schedule, etc.)


  - ai chat page ( after askes some questions then gaves doctor suggetions with the same speciality
   depending on te location of the patient , it gives a health advices and expect the ill )

   -reservation page


  - login page (ther is a red cells with animation in the background)
  - register page for doctors (ther is a red cells with animation in the background)
  - register page for patients (ther is a red cells with animation in the background)
  - profile page 


it is responsive and mobile-first 
there is a dark mode and light mode 
use colors(
    #BDC6B8,
    #EBF5EE,
    #BCE0DA,
    #921224
)
the website contains pay (so it contains a bugget and diposit metheods (syriteel cash))
the style is from tailwind